const blobService = require('./blobService');
const { cognitiveService, textAnalyticsService } = require('./cognitiveService');

module.exports = {
    blobService,
    cognitiveService,
    textAnalyticsService
};
